# Apple Calendar MCP

Read-only Apple Calendar MCP server with indexed archive search.

This package is developed in the `apple-app-mcp` repository. See
`CALENDAR.md` at the repository root for domain architecture, testing, and
development guidance.
